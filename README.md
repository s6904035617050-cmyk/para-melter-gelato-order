# Para-Melter Gelato Order System

ระบบสั่งไอศกรีมเจลาโต้ผ่าน QR Code พร้อมหน้าจอบาร์ตักเจลาโต้ Realtime

> **Note:** โปรเจกต์นี้ใช้ Next.js App Router เวอร์ชันล่าสุด ซึ่ง `params` ของ Dynamic Route เป็น Promise ต้อง unwrap ด้วย `use()` จาก React เสมอ (`const { tableNumber } = use(params)`)
